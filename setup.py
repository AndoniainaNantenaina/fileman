from setuptools import setup, find_packages

setup(
    name='fileman',
    version='0.1',
    description='A simple Python library for managing files and directories',
    author='Andoniaina Nomenjanahary',
    packages=find_packages(),
)
